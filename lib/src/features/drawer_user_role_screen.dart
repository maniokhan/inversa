import 'package:flutter/material.dart';

class DrawerUserRoleScreen extends StatelessWidget {
  const DrawerUserRoleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
